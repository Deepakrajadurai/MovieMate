<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovieMate" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>



    <!-- Define YourCustomParentTheme separately -->
    <style name="YourCustomParentTheme">
        <!-- Define attributes for YourCustomParentTheme here if needed -->
    </style>

    <!-- Then define Theme.MovieMates.MyCustomTheme, inheriting from YourCustomParentTheme -->
    <style name="Theme.MovieMates.MyCustomTheme" parent="YourCustomParentTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Other items -->
    </style>
            <!-- Base application theme. -->
            <style name="Theme.MovieMates" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
                <!-- Primary brand color. -->
                <item name="colorPrimary">@color/purple_500</item>
                <item name="colorPrimaryVariant">@color/purple_700</item>
                <item name="colorOnPrimary">@color/white</item>
                <!-- Secondary brand color. -->
                <item name="colorSecondary">@color/teal_200</item>
                <item name="colorSecondaryVariant">@color/teal_700</item>
                <item name="colorOnSecondary">@color/black</item>
                <!-- Status bar color. -->
                <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
                <!-- Customize your theme here. -->
            </style>

            <!-- You might have a specific style named "Theme" -->
            <style name="Theme" parent="Theme.MovieMates">
                <!-- Further customizations specific to "Theme" -->
            </style>

        <!-- Theme for MovieDetailActivity with no action bar -->
        <style name="Theme.MovieMate.NoActionBar"
            parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <!-- You can customize this theme further if needed -->
            <!-- For example, inherit other attributes from your base theme -->
            <item name="colorPrimary">@color/purple_500</item>
            <item name="colorPrimaryVariant">@color/purple_700</item>
            <item name="colorOnPrimary">@color/white</item>
            <item name="colorSecondary">@color/teal_200</item>
            <item name="colorSecondaryVariant">@color/teal_700</item>
            <item name="colorOnSecondary">@color/black</item>
            <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        </style>

    <!-- Base application theme for Day mode. -->
    <!-- This will be the main theme referenced in the Manifest for the application or specific activities. -->
    <!-- It inherits from a NoActionBar parent, so Activities using this theme directly will not have a default ActionBar. -->
    <!-- <style name="Theme.MovieMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!- Common attributes for both Day and Night if not overridden in values-night -->
        
        <!-- Base application theme. -->
        <!-- <style name="Theme.MovieMates" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
            <!- Primary brand color. -->
            </resources>
